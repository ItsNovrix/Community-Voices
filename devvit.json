{
  "$schema": "https://developers.reddit.com/schema/config-file.v1.json",
  "name": "community-voices",
  "permissions": {
    "reddit": true,
    "redis": true
  },
  "server": {
    "dir": "dist/server",
    "entry": "index.cjs"
  },
  "triggers": {
    "onAppInstall": "/internal/triggers/on-app-install",
    "onAppUpgrade": "/internal/triggers/on-app-upgrade",
    "onPostSubmit": "/internal/triggers/on-post-submit",
    "onCommentCreate": "/internal/triggers/on-comment-create"
  },
"scheduler": {
    "tasks": {
      "upgrade_notifier_job": {
        "endpoint": "/internal/scheduler/upgrade-notifier-job"
      }
    }
  },
  "marketingAssets": {
    "icon": "assets/icon.png"
  },
    "settings": {
    "subreddit": {
        "vipDescriptor": {
        "type": "string",
        "label": "VIP Descriptor Text",
        "helpText": "Define how your VIP users are described in your community. This is used in the header of pinned summary comments: 'There are X comments from [Descriptor] in this post'",
        "defaultValue": "VIP users"
      },
      "vipFlairs": {
        "type": "string",
        "label": "VIP User Flairs",
        "helpText": "Comma-separated list of flairs that trigger the bot (e.g., Pilot, Developer, Admin)"
      },
      "vipUsernames": {
        "type": "string",
        "label": "VIP Usernames",
        "helpText": "Comma-separated list of specific usernames to treat as VIPs (no u/ needed)"
      },
      "ignoreCommand": {
        "type": "string",
        "label": "Ignore Command",
        "helpText": "If a VIP starts their comment with this command, the bot will skip it.",
        "defaultValue": "!ignore"
      },
      "pinnedSummary": {
        "type": "boolean",
        "label": "Pinned Summary Comment",
        "helpText": "Enable to add a pinned comment summarizing all comments made by VIP users.",
        "defaultValue": true
      },
      "setFlairAfterCommenting": {
        "type": "boolean",
        "label": "Update Post Flair on Comments?",
        "helpText": "Enable to automatically update the post flair after a VIP user comments.",
        "defaultValue": false
      },
      "vipCommentPostFlairText": {
        "type": "string",
        "label": "Post Flair to Use",
        "helpText": "Enter the post flair to apply after a VIP user comments."
      },
      "setFlairAfterPosting": {
        "type": "boolean",
        "label": "Update Post Flair on VIP Posts?",
        "helpText": "Enable to automatically update the post flair after a VIP user submits a post.",
        "defaultValue": false
      },
      "vipPostFlairText": {
        "type": "string",
        "label": "Post Flair to Use",
        "helpText": "Enter the post flair to apply after a VIP user submits a post."
      },
      "autoStickyVipPosts": {
        "type": "select",
        "label": "Auto-Sticky Posts Made by VIP Users?",
        "options": [
          { "label": "Do Not Sticky", "value": "none" },
          { "label": "Slot 1", "value": "1" },
          { "label": "Slot 2", "value": "2" }
        ],
        "defaultValue": "none"
      }
    }
  },
  "dev": {
    "subreddit": "NovrixLabs"
  }
}